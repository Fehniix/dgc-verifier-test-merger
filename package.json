{
  "name": "csv-results-merger",
  "version": "1.1.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npm run clean && tsc",
    "start": "npm run build && node ./dist/index.js",
    "mon": "nodemon ./src/index.ts",
    "clean": "rm -r -f dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Johnny Bueti",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^17.0.17",
    "ts-node": "^10.5.0"
  }
}
